events {}

http {
    upstream app {
        server app_blue:5000;
        # server app_green:5000;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://app;
        }
    }
}